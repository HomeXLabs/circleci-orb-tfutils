description: >
  Using tflint command to see if all of the terraform files in the repository have the correct syntax.

steps:
  - run:
      name: Check with TFLint
      command: |
        /usr/local/bin/tflint
